// Inclui no compilador esses cabeçalhos que contém códigos reutilizáveis.
// .h => header(cabeçalho).
#include <stdio.h>
#include <stdlib.h>

// Funções precisam ser declaradas antes do main.
// Ex: void escrevaNome(); // E, posteriormente, eu posso escrever o código dessa função. // Protótipo de função
// Pincipal função na linguagem C, que vai ser necessária na hora da compilação.
void main() {
    // typedef enum {true=1, false=0} test;
    // printf(test);

    // Arquivo soma.c
    // soma();

    // Arquivo sizeof.c
    // typesize();

    // Arquivo especificadores_de_conversão.c
    // spec_conversor();

    // Arquivo sequencias_de_escape.c
    // sequences();

    // Arquivo scan_keyboard_data.c
    // keyboard_data(); // scanf[

    // Arquivo operadores_aritmeticos.c
    // operadores();

    // Arquivo calculadora.c
    // calculadora();

    // Arquivo estrutura de repetição.c
    // repeat();

    // Arquivo bubblesort.c
    test_bubblesort();
}
